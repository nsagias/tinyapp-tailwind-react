import HeroContainer from "./HeroContainer";

export default function Hero() {
  return (
    <section id="hero">
      {/* Hero Container */}
        <HeroContainer>
          {/* Content Container */}
          {/* Image Container */}
        </HeroContainer>
    </section>
  );
}